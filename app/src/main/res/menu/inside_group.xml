<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/actionBarGroup">

    <item
        android:id="@+id/addFilmGroup"
        android:icon="@drawable/ic_add_circle_24dp"
        android:title="Add Film"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/addUserGroup"
        android:icon="@drawable/ic_groups_white"
        android:title="Add Member"
        app:showAsAction="ifRoom"/>

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/membri_gruppo"
        android:title="Group Members"
        app:showAsAction="never"/>
    <item android:id="@+id/aggiungi_membro"
        android:title="Add Member"
        app:showAsAction="never"/>
   <!-- <item android:id="@+id/silenzia_notifiche"
        android:title="Silence Notifications"
        app:showAsAction="never"/>-->
    <item android:id="@+id/lascia_gruppo"
        android:title="Delete and Leave Group"
        app:showAsAction="never"/>
</menu>